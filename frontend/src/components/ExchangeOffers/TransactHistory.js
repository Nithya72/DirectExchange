import React, {Component} from "react";
import axios from "axios";
import jwt_decode from "jwt-decode";
import {Redirect} from "react-router";

export default class Home extends Component {

  render(){
    return(
        <div>Inside history</div>
    )
  }
}